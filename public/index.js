// TODO: make the Form send compatible information to the `/play/` route
// The `/play/` route works as follows:
// `/play/180000/YkADj0TPrJA/1514241540000`
// the first parameter is the timestamp in milliseconds (mandatory)
// the second parameter (optional) is the Youtube video id (will default to phil collins)
// the third parameter (optional) is the time in milliseconds when the timestamp should play at (will default to the new year posix)
// the Form should make a redirect to the `/play/` route with compatible information

// TODO: add UX
